# Tasks: add-sandbox-core

Order follows the migration plan (design): behavior-neutral port refactor
first, container mechanics second, egress and hardening third, E2E last.
Before group 4: re-verify tool state per Q1 (mitmproxy SNI mode, Docker
ladder review note in explore notes).

## 1. Environment port and host adapter (behavior-neutral)

- [x] 1.1 Define `TaskExecutionEnvironment` port (materialize with optional factory-chosen commit pin + per-environment scratch area / exec with optional stdin / putFile / readFile with size cap, paths under working copy or scratch only / harvest / dispose, streamed exec result; file ops boundary-only; no snapshot op) with javadoc traceability (FR1, NFR-S3)
- [x] 1.2 Define adapter capability passport model and `SecretsProvider` port with env/file adapter (FR14, FR18, NFR-S1, D12)
- [x] 1.3 Extract host adapter over existing worktree + `ProcessBuilder` mechanics; passport declares no isolation (FR2)
- [x] 1.4 Route `AgentProcessLauncher` and `CommandProcessRunner` consumers through the port; forbid direct spawning by design (FR4)
- [x] 1.5 Port-level contract spec suite (Spock) run against the host adapter; existing app/e2e specs stay green (M1)
- [x] 1.6 Route tracker-token resolution through `SecretsProvider` (github-tracker delta); `GNOMISH_GITHUB_TOKEN` becomes the env adapter's backing name; credential names are refused in child-env allowlists (FR18, NFR-S1)
- [x] 1.7 Prompts via stdin in launcher and judge voter (all modes); update fake-agent contract specs — the one deliberately non-behavior-neutral point of this group (FR24, D18)

## 2. Pipeline config: sandbox declarations

- [x] 2.1 Extend stage model + loader with Mechanism sandbox declarations: needs, `requires-fresh`, `verify-in` (FR12, FR13)
- [x] 2.2 Tighten-only validation: reject host/adapter requests and weakenings as located `ConfigError`s (FR14)
- [x] 2.3 Loader specs: declarations load typed; violations reported; defaults applied (FR12–FR14)
- [x] 2.4 External check declaration gains optional pin paths (definition files of the check); repo-relative data, never read by the loader, exempt from the `.gnomish/` traversal rule; reject absolute / non-normalized (`.`, `..`) forms as located `ConfigError`; typed into the model, loader specs (FR16)
- [x] 2.5 Pipeline-law binding per invocation: law read at invocation start from the factory clone of the base branch (git modes) or the workspace snapshot (in-place), frozen for the invocation; control files and judge criteria read from the law source, never lazily from the working copy (`ControlFilePreflight` rework); contract test proves gnome-branch edits never affect the running task (FR19, NFR-S2, D14)

## 3. Binding resolution and reconciliation

- [x] 3.1 Factory config: adapter bindings per stage, `factory.sandbox.image`, `factory.sandbox.runtime`, resource limits, egress allowlist, child-env passthrough names (FR3, FR7, FR9, FR10)
- [x] 3.2 Needs-vs-passport reconciliation, fail-closed with named unmet need; container default binding, no silent host fallback (FR14, G2, UX2)
- [x] 3.3 Segment computation: contiguous equal-binding stages share an environment; boundary = harvest → dispose → materialize; `requires-fresh` splits segments (FR12, FR13, NFR-P1)
- [x] 3.4 Specs for reconciliation, defaulting, and segment lifecycle incl. in-segment reuse without re-materialization (M1, NFR-P1)

## 4. Container adapter

- [x] 4.1 Docker CLI runner (subprocess, like git): create/inspect/remove network, volume, container; factory labels on every object (FR3)
- [x] 4.2 `exec` into the container with streamed stdout/stderr + exit code; workdir in the volume working copy (FR3, FR4)
- [x] 4.3 Resource limits from config on `run` (`--cpus`, `--memory`, `--pids-limit`, volume size) (FR10)
- [x] 4.4 Idempotent `dispose` (container+volume+network); startup orphan sweep by label (FR11, NFR-R2) — `ContainerOrphanSweeper` wired into the container runner-start path via `ContainerEnvironments.sweepOrphans()` → `ContainerRunSupport.sweepOrphans()`, called at the start of `ContainerTerminalDrive.run` (the single chokepoint both the fresh run and every runnable resume outcome pass through, after the cheap git pre-flight refusals — duplicate-branch, no-branch, aborted, completed-report — so those never touch Docker); the sweep keeps this task's three role keys (`<key>`/`-j`/`-v`) so a reattaching resume is never swept, and a missing daemon is a logged no-op. The `serve`-start sweep stays with the serve/lifecycle pass (4.8 precedent), where the held-key set spans all in-flight tasks
- [x] 4.5 Contract suite from 1.5 passes against the container adapter (Docker-gated integration spec) (M1)
- [x] 4.6 Read-only control surfaces inside the container: agent-CLI config, shell rc files, baked proxy/CA/build configs root-owned or `:ro`-mounted; spec asserts in-box writes to them fail (FR20)
- [x] 4.7 Runtime-outage classification: Docker daemon unavailable at materialize or mid-task = infrastructure failure, no attempt burned; persistent outage escalates cannot-execute (NFR-R1)
- [x] 4.8 Keep semantics for ended tasks: stop the container, retain volume + network; serve cleaner disposes aged environments via the port using runtime-metadata age (factory-serve delta, NFR-R2) — adapter mechanism landed additively (`ContainerEnvironmentReaper.stopKeeping`/`reapAged` measuring runtime finished-at, disposing via the `ContainerEnvironmentDisposal` port seam); daemon scheduling and the task-end `stopKeeping` call are deferred to the serve/lifecycle-integration pass

## 5. Git mechanics: clone, harvest, salvage

- [x] 5.1 Materialize = `git clone --no-hardlinks` from the factory clone into the environment; agent identity, `gc.auto 0`, no remote/creds inside (FR3)
- [x] 5.2 Harvest = fixed-refspec ff-only `--no-recurse-submodules` fetch from the environment; precedes any push; rate-limited tip polling (event-driven detection, if enabled, watches `.git/logs/HEAD` — not packable refs — and only wakes the poller) (FR5) — transport = `ext::docker exec -i <box> %S /gnomish/work` (`protocol.ext.allow=user` granted per invocation); event-driven detection not implemented, poll-only per the "if enabled" latitude
- [x] 5.3 Resume materializes through the bound environment from the branch alone; salvage via in-box commit + harvest, with lost-environment fallback; the tracker-take revocation path reuses the same salvage → harvest → factory-side push mechanics (FR6, tracker-take delta) — mechanics landed: reattach-aware `materialize` (start stopped box / recreate over surviving volume / fresh clone fallback), `EnvironmentSalvage` with loss-tolerant degradation, `TaskSalvage` seam adopted by `RevocationHandler`; container-mode app wiring follows with the integration pass (4.8 precedent)
- [x] 5.4 Factory git hardening: bare-object reads of gnome branches, empty `core.hooksPath` on all factory-managed copies (FR17)
- [x] 5.5 Specs with local bare repos: no-hardlink independence, rewritten-history refusal, hooks don't cross, salvage paths, resume-by-second-instance (M4) — `ContainerGitMechanicsSpec` (Docker-gated, local repos as factory clones/origin) + daemon-free `ContainerHarvestFetchSpec`/`MidRoundHarvestListenerSpec`
- [x] 5.6 Snapshot-first round protocol: in-box snapshot commit (hooks off, as gnome user) → harvest → verification against the attempt commit → state files via putFile → state commit → harvest with read-back and parent-check; resume treats snapshot-without-state as interrupted verification, no attempt burned (FR21, FR22, D15, D16) — components: `EnvironmentRoundSnapshot` (executor-tail seam) + `AttemptCommitRef` + `EnvironmentAttemptPersistence` (parent-check, byte-exact `gitobjects` read-back) + `SnapshotTipCheck`; executor/engine wiring follows with the integration pass (4.8 precedent)
- [x] 5.7 Boundary verification factory-side on harvested refs with the `decisions/<stage>-a<attempt>.json` carve-out; in-branch decision protocol wired through `$GNOMISH_DECISION_FILE` with stale-name exclusion (FR21, FR23, D17) — `HarvestedBoundaryCheck` (run inside sandboxed persist) + `BranchDecisionFile` (working-copy-relative `$GNOMISH_DECISION_FILE`, boundary-time channel read, path identical to the carve-out by construction); `CliStageExecutor` adoption of the branch transport in sandboxed mode follows with the integration pass
- [x] 5.8 `gitobjects` library package (top-level, beside `domain`/`app`/`adapter`): `GitObjects` facade (resolveRef / readBlob with size cap / commit), sealed `TreeEdit` (PutFile mode 100644 / DeletePath), temp-index plumbing chain (read-tree → hash-object --stdin → update-index → write-tree → commit-tree), atomic CAS `update-ref`, path validation (no absolute paths, no `..`, no `.git/**`), caller-supplied identity/timestamps/message; JDK + SLF4J API only — no factory, Spring, or Jackson imports; ArchUnit boundary rules in both directions (introduces ArchUnit per ADR 0001) (FR25, D19)
- [x] 5.9 `gitobjects` Spock specs on real temp repositories: works on a bare repo (no-checkout proof), hooks never fire, stale-tip CAS refusal leaves the ref unchanged, untouched subtrees keep identical tree hashes, directory delete preserves history, path-validation refusals, byte round-trip and read size cap, deterministic commit ids from fixed metadata, temp-index cleanup on failure (FR25, D19)
- [x] 5.10 Route sandboxed lifecycle writes through `gitobjects`: branch creation with `task.json`, resume decision before materialize, outcome recording incl. aborted-on-last-harvested-tip with a dead or quarantined box, Completed outcome + cleanup after dispose; host mode unchanged (FR25, D19)

## 6. Egress guard and self-check

- [x] 6.1 Guard lifecycle: mitmdump (SNI/CONNECT mode, no TLS opening) as a factory-managed container in task network + bridge; allowlist rendered from config; DNS via guard (FR7) — `EgressGuard` + `GuardCommands` + `EgressGuardConfig` (addon denies non-allowlisted CONNECT/plain-HTTP with 403, forwards TLS unmodified via `tls_clienthello.ignore_connection`; `connection_strategy=lazy`); new `factory.sandbox.guard-image` knob (default `mitmproxy/mitmproxy:12`, verified current); guard covered by disposal and orphan sweep; materialize-time wiring follows with the integration pass (4.8 precedent)
- [x] 6.2 Structured denial log → findings in the task report (metadata only) (NFR-O1) — `GuardDenialLog` (marked JSONL from `docker logs --tail`, field/event caps, tolerant parse) surfaced via `EgressGuard.denialFindings()`; attachment to the tracker report rides the findings-funnel wiring (task 8.2)
- [x] 6.3 In-box self-check before the first gnome-product process in every materialized environment (round and fresh-box verification/judge alike; the probes themselves run via exec): direct egress fails / non-listed denied / listed passes / isolation matches passport; failure = infrastructure failure (FR8) — `EnvironmentSelfCheck` + `SelfCheckFailedException` (names the failed probe, UX2); network probes exec `curl` in-box (image contract: curl baked, task 9.1), isolation asserted from runtime metadata (`--internal` flag, runtime vs config, passport); call-before-first-process wiring follows with the integration pass
- [x] 6.4 Guard outage classification as infrastructure failure + guard restart (NFR-R1) — `EgressGuard.ensureRunning()` converges (start stopped / recreate broken once) with a readiness retry in the first proxied probe; `GuardUnavailableException` classified exactly like `DockerUnavailableException`
- [x] 6.5 Specs: allowlist enforcement (WireMock/local guard), self-check verdicts, denial findings (M2) — daemon-free unit specs (`EgressGuardSpec`, `EnvironmentSelfCheckSpec`, `GuardDenialLogSpec`, `EgressGuardConfigSpec`, `GuardCommandsSpec`) + Docker-gated `EgressGuardIntegrationSpec`: real mitmdump guard, real box, production self-check end-to-end with a local HTTP target as the allowlisted destination, denial read-back, stopped-guard restart; scoped PIT green at 100%

## 7. Env allowlist

- [x] 7.1 Layered allowlist construction for both adapters: child env = adapter base ∪ passthrough names (values live from the factory env at exec) ∪ factory-set vars; container base empty (`--env` only — the image supplies runtime env), host base = fixed documented set via `environment().clear()` + puts; rewrite `AgentProcessLauncher`'s javadoc, which documents the replaced inherit-everything-minus-scrub behaviour; credential names in passthrough refused as `ConfigError`; applied names (never values) logged at debug (FR9, D6) — `ChildEnvAllowlist` (compose seam; passthrough∩credential refused at construction as a startup `IllegalArgumentException` naming the variables — application-properties errors have no located-`ConfigError` file context; declared credential names additionally excluded from every composed map as the scrub-last twin); operator passthrough = `factory.sandbox.env-passthrough` (the older `factory.agent-cli-env-passthrough`, documentation-only under inheritance, is marked superseded/ignored); allowlist built per run in `ManualRunAssembly.assemble` from passthrough + the tracker's declared credential names (the app-layer `credentialEnvVarsToScrub` threading is unchanged); the replaced inherit-minus-scrub javadoc lived in `HostTaskExecutionEnvironment`/`ExecCommand` after `AgentProcessLauncher`'s earlier removal and is rewritten there; the AI base-url/auth-token seam (`AgentAiSeam`: `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_MODEL`, read live) is re-set by the agent adapters as factory-set protocol vars, keeping the Ollama E2E seam working without inheritance
- [x] 7.2 Specs asserting no factory env var leaks into `exec` children in either adapter (M3) — host: real-child `env`-dump subset proof + live-passthrough + credential-exclusion specs in `HostTaskExecutionEnvironmentSpec`; container: composed `--env`-entries-only argv proof in `ContainerTaskExecutionEnvironmentUnitSpec`; check runner leak spec in `ShellCommandCheckRunnerSpec`; `ChildEnvAllowlistSpec`/`AgentAiSeamSpec` for the components; the in-JVM `TakeCommandCredentialScrubSpec` wiring proof holds unchanged (M3 E2E remains task 9.3)
- [x] 7.3 Findings-file path allocated in the environment's scratch area and read back through `readFile` (size-capped) in both adapters; container reads stream via exec, never host-side archive extraction (FR1, NFR-S3) — `ShellCommandCheckRunner` materializes its (still host-local, integration-pass precedent of 4.8) environment, allocates `scratchRoot()/findings-<uuid>.json`, reads back via `readFile` with a 256 KiB cap, disposes in `finally`; `FindingsFileReader` is byte-based and filesystem-free; the container leg is the already-landed exec-streaming `ContainerFileChannel`, exercised by the port contract suite

## 8. Verification hardening

- [x] 8.1 Findings funnel component: strict verdict schema (judge parse failure = infra failure), size caps at read/poll, ANSI/control sanitization, fenced tracker rendering with escaped mentions (FR15) — `adapter/findings` package (`FindingsSanitizer` strip/forLog/capTail, `TrackerFence` labeled tilde fence with dynamic length + `@`-ZWSP mention escaping, resolving design Q3 adapter-side); `JudgeVerdictExtractor` gains the strict findings-entry schema (non-string/blank entry → CannotVerify) and a sanitized WARN log
- [x] 8.2 Route judge/external/command findings through the funnel; engine branching untouched (FR15, Q3) — `GithubWorkflowJobsFetcher` strips+caps via the funnel (stand-in javadoc retired), `EscalationResumeDialog.renderEscalation` publishes `CannotVerify.details` (the one check-output-carrying report field) fenced; command findings keep their read-cap + strict `FindingsFileReader` schema; data sinks (state.json, retry feedback) stay full-fidelity by design
- [x] 8.3 Pin-check guard component wrapping any `ExternalCheckClient`: pin set = law-declared paths ∪ adapter-contributed paths; byte-compare vs base branch as bare object reads at the attempt commit; diff = Fail with diff findings, adapter never invoked; empty union passes vacuously (FR16, D10) — `PinCheckedExternalCheckClient` + `ExternalCheckPinContributor` seam + `AttemptCommitWorkspace` (the concrete engine workspace carrying `AttemptCommitRef`, D15); comparison via `GitObjects.readBlob` under a 1 MiB cap; non-commit workspace / unresolvable base / oversized pin degrade fail-closed to CannotVerify; spec on real temp repos incl. the early-substitution scenario
- [x] 8.4 Guard wired into every assembly that binds an external check client (interactive included); contract case in `ExternalCheckClientContract`: pinned-diff refusal and vacuous pass; early-substitution-caught-at-point-of-use spec (FR16, D10) — also close the wiring gap carried from add-external-check-github-actions Q5: construct `GithubCheckExternalClient` from factory config (base-URL config key, `SecretsProvider` token) and inject it into the stage engine behind the guard, making the adapter operator-enableable; replace the adapter-local `GithubCheckWorkspace` stand-in with the concrete workspace type carrying the attempt commit; `GNOMISH_GITHUB_ACTIONS_TOKEN` becomes the env adapter's backing name for the external-check token and is declared as a credential name refused in child-env allowlists (FR26, D15, github-external-check delta) — `ManualRunAssembly.externalCheckClient` (the single injection site take/serve reuse) always wraps in the guard, comparing against the law-source clone's `HEAD` (= the bound base branch, D14); `factory.check.github.{api-url,repo}` (both-or-neither validated) + `GithubCheckClientFactory` (SecretsProvider token, fail-closed; `pinContributor()`); `GithubCheckWorkspace`/`GithubCheckToken` deleted in favor of `AttemptCommitWorkspace`; guard proven contract-transparent by `PinCheckedExternalCheckClientContractSpec`, pin scenarios in `PinCheckedExternalCheckClientSpec`
- [x] 8.4a Extend `PollStatus.Pass` to carry the platform run URL (domain-model change) and preserve it into the recorded check result, so a green external check's run link reaches the tracker report the same way a Fail's does today; adapter fills it from the authoritative run; closes add-external-check-github-actions Q4 (NFR-O2, stage-engine + github-external-check deltas) — `PollStatus.Pass(runUrl?)`/`Verdict.Pass(runUrl?)` with no-arg convenience ctors, `ExternalPolling` maps it through, `GithubWorkflowRunVerdict` fills it from the run's `htmlUrl`; persisted additively as `runUrl` across state.json/status.json/usage.json check DTOs (reference JSON updated)
- [x] 8.4b External-check push precondition: before the poll loop starts, verify the attempt commit is delivered to the remote, re-attempting the push; an undeliverable commit classifies the check as CannotVerify (infrastructure failure, no attempt burned), never a poll-timeout quality failure (FR21, stage-engine delta) — `AttemptDelivery` engine port (10th `EnginePorts` component; 9-arg convenience ctor defaults to `assumedDelivered()` for the in-place/interactive shapes) checked by `ExternalPolling` before the loop; `RemoteAttemptDelivery` git adapter (remote-tip ancestry first, then a once-retried `origin branch:branch` push, no-remote = undeliverable) landed additively — sandboxed assemblies adopt it with the integration pass (4.8 precedent), since host mode has no attempt commit at verify time
- [x] 8.5 Model-output write confinement: symlink-resolving path guard, `.git/**` refusal, contract test (FR17) — the confinement lives at the file channel both adapters share (any model-output application inherits it by construction): `ChannelPathResolver` refuses `.git/**` on the normalized path and the symlink-resolved anchor alike, `ContainerFileChannel.validate` refuses it lexically (in-box symlink resolution deliberately absent per D16); contract cases (`putFile`/`readFile` of `.git/**`, traversal) + host symlink-into-.git spec
- [x] 8.6 Funnel/pin-check/confinement specs incl. injection, escape-sequence, truncation, traversal cases (FR15–FR17) — written alongside each component: `FindingsSanitizerSpec` (CSI/OSC/C1/CR stripping, tail-cap boundaries), `TrackerFenceSpec` (mention escaping, fence-breakout, inert injection), `JudgeVerdictExtractorSpec` strict-schema + sanitized-WARN cases, `RunnerOutcomeLoopSpec` fenced-escalation case, `PinCheckedExternalCheckClientSpec` (+ contract binding), environment-contract `.git/**`/traversal cases + host symlink-into-.git spec, `JudgePromptBuilderSpec` delimiter-integrity cases
- [x] 8.7 Judge hardening: input artifacts wrapped in hard delimiters as data; sandboxed judge votes run in a fresh environment materialized from the attempt commit (votes of one attempt may share it); specs for delimiter integrity and fresh-box judging — engine vote/majority mechanics untouched (FR15, D9) — `JudgePromptBuilder` wraps the task context (the prompt's artifact-derived content) in growing hard delimiters with ignore-instructions framing; `JudgeEnvironmentSource` seam on `CliJudgeVoter` (host default unchanged) + `FreshJudgeEnvironments` (per-attempt fresh box pinned at the attempt commit, shared across votes, disposed on attempt change); container-mode wiring follows the integration pass (4.8 precedent)
- [x] 8.8 Builtin checks (`files_exist`, future schema checks) evaluate the attempt commit via bare object reads in sandboxed mode — one implementation for all adapters; host modes keep filesystem checks (FR21, D15) — `GitObjects.exists` (`cat-file -e`, blob-or-tree, path-validated) + `FilesExistCheckRunner.withAttemptReader` rebind with an `AttemptCommitWorkspace` leg (unsafe path → CannotVerify, the host leg untouched); real-repo specs prove box residue never counts; per-run reader binding rides the sandbox integration pass

## 9. E2E, image, docs

- [x] 9.1 Reference image recipe: JDK, git, agent CLI, baked CA, JVM/Gradle proxy configs, registry endpoints as build args (UX4, D7) — `docs/examples/sandbox-image/` (Dockerfile + README + `ca/` seam): Temurin base as `BASE_IMAGE` arg, npm/Maven registry and proxy address as build args, root-owned `/etc/gnomish` build configs + `/etc/claude-code/managed-settings.json` policy surface (FR20), non-root `gnome` uid 1000 owning `/gnomish/**`; build + contract smoke-verified against a real daemon; enabled by a new stable guard network alias (`GuardCommands.PROXY_ALIAS` = `gnomish-guard`, `--network-alias` on the guard) so baked JVM/Gradle proxy configs dial one address in every environment — `EgressGuard.proxyUrl()` now returns the alias, so the self-check probes verify exactly the address baked configs use
- [x] 9.2 E2E (Testcontainers + Gitea): full pipeline in container mode — clone, rounds in box, harvest, outside push (M2) — carried the deferred **sandbox integration pass** the group-4–8 notes pointed at: `SandboxModeSelector` (bindings resolve fail-closed per D13 in `ManualRunRunner`'s git/resume dispatch; container default refuses without image/Docker naming the two ways out; mixed host/container bindings refused honestly; needs reconciled via `SandboxReconciler`), `ContainerEnvironments` (per-task env+guard+self-check construction, role keys `<key>`/`-j`/`-v`) + `SelfCheckedEnvironment` (materialize ⇒ self-check by construction, guard proxy env merged into every exec) + `EnvironmentLease`/`LeasedEnvironment` (segment lifecycle), executor seam `RoundEnvironmentSource` (host default extracted as `HostRoundEnvironmentSource`; `SandboxRoundEnvironmentSource` = leased box + `BranchDecisionFile` + `EnvironmentRoundSnapshot` tail + `MidRoundHarvestListener`), `CheckEnvironmentSource` on `ShellCommandCheckRunner` (same-box from the lease, `verify-in: fresh-box` from the attempt commit), `FilesExistCheckRunner.withAttemptReader` + `RemoteAttemptDelivery` + `FreshJudgeEnvironments` bound via `SandboxRunPieces`/`ManualRunAssembly.withSandbox`, `ContainerRunSupport`/`ContainerGitModeRunner`/`ContainerTerminalDrive` (fresh run over `GitObjectsTaskRepository` lifecycle commits, D19 ordering, keep-stopped on every non-completed exit, `PushBestEffortAttemptPersistence`); take/serve container adoption stays with the serve/lifecycle pass (4.8 wording). `ContainerModePipelineE2ESpec` (Docker+Gitea-gated) proves clone→round-in-box→snapshot/state→harvest→verification at the attempt commit (builtin bare-object + fresh-box command gate)→outside push→cleanup tip→disposed objects
- [x] 9.3 E2E: self-check catches broken isolation; in-box env holds no tracker token / host vars (M2, M3) — `ContainerModeIsolationE2ESpec`: a task network that lost `--internal` is rejected by the production self-check before any gnome process (probe named, UX2); an in-box `env` dump is a subset of image `ENV` + the guard proxy fragment — no credential names, no factory-process host variables
- [x] 9.4 E2E: kill mid-round, second instance resumes/salvages in container mode; kill between snapshot and state commit — resume re-verifies; pending decision request recovered from the branch (M4, FR21, FR23) — `ContainerResumeRunner` + `ContainerResumeBranch` (branch-alone bootstrap: locate/narrow-fetch, ref-level ff/diverged reconcile, bare-object task.json/state.json reads, salvage via reattached box, `--discard-work` = dispose + fresh materialize, decision appended factory-side before materialize) + `ResumeVerificationStageExecutor` (snapshot-without-state consumes the pending attempt commit, no agent re-run, no attempt burned); `ContainerModeResumeE2ESpec`: EOF-killed instance leaves a stopped kept box and the decision file on the branch (FR23), a second instance reattaches, salvages a planted leftover (`gnomish: salvage` commit), completes and disposes; the fabricated snapshot-tip case completes under a poisoned scenario (agent re-run would fail) with exactly one recorded round; fake-agent now `mkdir -p`s the decision file's parent like the real CLI's Write
- [x] 9.5 Operator docs: sandbox config, host-mode passport honesty (isolation: none; the env allowlist bounds variables, not filesystem access), egress-allowlist maintenance, host env passthrough recipes (`SSH_AUTH_SOCK` for private SSH dependencies; reward-hacking caution: a missing env var fails checks as a quality failure and can push the gnome to route around it), Docker-inside ladder (step 0 = CI external check; `gnomish/*` workflows without privileged secrets; step 1 neighbor stacks arrive with change D — see NG5), `verify-in: fresh-box` recommendation for final quality gates, limits tuning, interim threat-#45 guidance (disable provider-side web tools until change B) (UX1–UX5, NG5, NG7) — `docs/operator-guide-sandbox.md` (linked from the tracker operator guide), covering all of the above plus the two current boundaries stated honestly (mixed bindings refused; take/serve container adoption rides the serve/lifecycle pass) and the disk-quota opt-in (`factory.sandbox.enforce-disk-quota`, new key); `factory.bindings.default` now binds its documented key via `@Name` (the record component `defaultBinding` alone would bind `default-binding`)

## 10. Gates

- [x] 10.1 `./gradlew check` green: Spock, JaCoCo, PIT on changed Java scope, Error Prone/NullAway, Spotless — verified 2026-08-11: full `./gradlew check` passes (3363/3363 PIT mutations killed, 100% test strength); required fixing a real `:test`/`:pitest` Docker container-name race, excluding hang-prone Docker E2E specs from PIT, closing a genuine mutation-coverage gap in the container-mode integration layer with ~15+ new/extended Spock specs, and three `@DoNotMutate` exceptions for equivalent/unsafe-to-mutate boundaries
- [x] 10.2 Traceability grep: every FR/NFR/UX of add-sandbox-core has an implementing entity in code or specs — verified 2026-08-10: all FR1–FR26, NFR-S1–S3/R1–R2/O1–O2/P1/C1, UX1–UX6 appear (word-boundary grep) in `src`/`docs` files that also reference add-sandbox-core; no ID missing
