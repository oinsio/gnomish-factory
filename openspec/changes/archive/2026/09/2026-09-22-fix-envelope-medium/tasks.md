# Tasks: fix-envelope-medium

> Sequencing: starts after `type-untrusted-text` (committed as #58, archived 2026-09-21) and
> before `introduce-take-order`, which rewrites the same application call sites (proposal,
> header). Group 1 lands the red specs first — the report's reproduction (1.1, 1.2) must be red
> before group 2 touches the readers, and the task report records the run in which it was red.
> Groups 2–4 are the reader move; group 5 the gate; group 6 the durable record; groups 8–10 the
> three folded-in findings (path owner, excerpt bound, rename) — 8 must precede 5.1's literal
> rule, 9 and 10 are independent of everything else; group 7 closes. Each task's sub-agent
> report ends with the old-way sweep it ran (`.claude/rules/implementation.md`).

## 1. Red specs on the real medium (FR3, FR6, FR7, M4, M5)

- [x] 1.1 Add to `GitTaskRepositorySpec` the scenario "finishCleanup converges a removal a killed predecessor already staged": create and complete a task on a real clone, run `git rm -r .gnomish-task` in its worktree by hand without committing, call `finishCleanup`, and assert the tip's tree lacks `.gnomish-task/` and a second `finishCleanup` changes the tip's id no further; verify it is red today (the guard reads the worktree and skips) and the failure names the tip still carrying the directory (FR3, M4)
- [x] 1.2 Add to `FinishKillPoints` a fourth durable step for the host medium only — "the staged removal", `git rm -r .gnomish-task` in the world's worktree, no commit — declared so the container world's row keeps three steps (`KillPointWorlds` decides per medium); frozen shape `CompletedUncleaned`, pickup through the existing `FinishEffect` + `finishCleanup` on the same world and the same worktree, converged `Delivered`, worktree removed, second pass a no-op; verify `TransitionKillPointSpec` is red on the new row today (FR7, NFR-R1, M1)
- [x] 1.3 Add to `DeliveredBranchReaderSpec` two scenarios: "reads the delivered state from the Completed commit when the branch gained commits after cleanup" (append one plain commit after the cleanup commit, then `read`) and "reads at the tip when the tip still carries the envelope" (a `CompletedUncleaned` branch); verify both are red today — the first reads the wrong revision, the second reads `tip^` (FR6, M5)
- [x] 1.4 Add to `GitTaskStoreSpec` (`:adapters:git`, a real clone) the scenario "readTaskRecord answers the tip when a killed predecessor removed the envelope from the worktree": complete a task, run `git rm -r .gnomish-task` in its worktree by hand without committing, call `readTaskRecord(worktree)` and assert the `Completed` record the tip carries; verify red today (`UncheckedIOException(NoSuchFileException)` — the answer `HostResumeMechanics.loadBranch` turns into the delivered route). No spec in the repository drives `HostResumeMechanics` over a real `GitTaskStore` (`TakeResumeRoutingSpec` / `TakeResumeShapeTailSpec` stub `TaskStoreGit`; the kill-point host world drives `FinishEffect` directly), so the read is reproduced at the adapter seam here and the pickup end to end by 1.2; the stub-level routing scenarios are written in 3.1, after the port changes (FR1, FR5)

## 2. The tip reader serves the host medium (FR1, FR2, FR4, NFR-R2)

- [x] 2.1 Add `GitShowTip.carries(String path)` — `git cat-file -e <revision>:<path>` through `GitReadGate.answered`, true on exit 0 — and `GitShowTip.cleanupCommit()` returning `Optional<String>` (the located id from the existing `rev-list` walk), keeping `cleanupCommitInHistory()` as its `isPresent()` wrapper; extend `BranchTipSourceSpec` (the read answers) and `GitShowTipTerminationSpec` (the cut-off invocation) so a cut-off `cat-file` throws `BranchTipUnavailableException` rather than answering false; verify `:adapters:git:test` passes (FR4, FR6, NFR-R2)
- [x] 2.2 Change `TaskStoreGit.readRecordedState` / `readTaskRecord` to return `Optional<TaskState>` / `Optional<TaskRecord>` and reimplement both in `GitTaskStore` over `new GitShowTip(runner, worktree, GitObjects.HEAD)`'s `readAtTip`, deleting `GitTaskStore.read` and its `Files.readString`; replace the three existing `GitTaskStoreSpec` read scenarios (`readRecordedState reads the worktree's state.json…`, `readTaskRecord reads the worktree's task.json…`, `reading a state file that is not there fails loudly…`) with five over a real clone (present at the tip; absent at the tip → empty; worktree file differs from the tip → the tip's value wins; a cut-off `git show` → `BranchTipUnavailableException`; each read spawns exactly one git invocation and no filesystem read — through a recording git stand-in binary as `BaseRefreshCloneSafetySpec.recordingGit` does, M9); verify `:adapters:git:test` passes and `grep -n "Files\." adapters/git/src/main/java/com/github/oinsio/gnomish/adapter/git/GitTaskStore.java` is empty (FR1, NFR-R2)
- [x] 2.3 Route the three lifecycle rewrites through the tip: `TerminalWriteMarker.clearPending` and `GitTaskRepository.readCurrentDto` read the DTO from `GitShowTip.readAtTip(TASK_JSON_PATH)` at `HEAD` (absent → `GitTaskRepositoryException` as today's I/O failure did), `StateFileWrite.currentCursor` reads the cursor from `readAtTip(STATE_JSON_PATH)` and degrades to `null` as today; extend `GitTaskRepositorySpec` — the spec that drives `confirmTerminalWrite`, the outcome/decision rewrites, and the cursor carry-forward — with one scenario per site where the worktree file was altered by hand and the commit still carries the tip-derived value; verify `:adapters:git:check` including PIT passes with no new exemption (FR2)
- [x] 2.4 `CleanupCommit.commit`: guard on `GitShowTip.carries(DIR_NAME)` at `HEAD`; run `git rm -r --ignore-unmatch <DIR_NAME>`; when `rm` reports nothing removed while the guard held, log the INFO line of NFR-O1 (`cleanup commit for task {} lands a removal a predecessor already staged`); commit as today; rewrite the class javadoc's kill-window paragraph to the four-row matrix of design D4 and the "Kept in sync with" marker to "both test the tip"; verify 1.1 is green and `GitTaskRepositorySpec`'s existing "finishCleanup on an already-cleaned tip changes nothing" still passes (FR3, NFR-O1)
- [x] 2.5 `WorktreeSalvage.restoreFactoryFiles`: replace the inline `cat-file -e HEAD:<STATE_DIR>` with `GitShowTip.carries` at `HEAD`, adjusting the "Kept in sync with" marker text; verify `WorktreeSalvageSpec` passes and `grep -rn '"cat-file", "-e", "HEAD:' adapters/git/src/main` is empty outside `GitShowTip` — the remaining `cat-file` hits are the exemptions of the single-owner table (`TaskBranchCreator`'s commit-existence probe, `EnvironmentSalvage`'s in-container script, `FactoryCloneHardening`'s javadoc) and are listed as such in the task report (FR4)
- [x] 2.6 `DeliveredBranchReader`: resolve the revision as design D5 — the tip when `readAtTip(TASK_JSON_PATH)` at the tip is present, else `cleanupCommit() + "^"`, else `BranchStateFileMissingException` — and delete the `tip + "^"` literal and the two javadoc paragraphs recording the task 6.5 concern; verify 1.3 is green and the existing four `DeliveredBranchReaderSpec` scenarios still pass (FR6, M5)

## 3. Application call sites carry typed absence (FR1, FR5)

- [x] 3.1 `HostResumeMechanics`: `loadBranch` returns `null` on an empty `readTaskRecord` (the delivered route), `readFinalState` returns the first-stage state on an empty `readRecordedState` (the pre-contract tip); delete both `catch (UncheckedIOException)` arms and the `NoSuchFileException` import; rewrite the `TakeResumeRoutingSpec` / `TakeResumeShapeTailSpec` scenarios that stubbed `UncheckedIOException(NoSuchFileException)` to stub `Optional.empty()` instead (empty `readTaskRecord` → delivered route; empty `readRecordedState` → first-stage state; present `Completed` record → the uncleaned finish); verify 1.4 is green and both specs pass (FR5)
- [x] 3.2 `TakeResumeBootstrap.bootstrap` returns `Optional<ResumeBootstrap>` (empty on an empty `readTaskRecord`) and `TakeResumeRunner.bootstrap` returns it as `@Nullable` — this pair MUST NOT unwrap, because it is the only path `HostResumeMechanics.loadBranch` reaches the tip through and 3.1's delivered route is the empty value (FR5); then `TakeFreshClaim`, `GitResumeRunner.bootstrap` (the manual-run caller of `TakeResumeBootstrap`, which has no delivered route) and `continueFrom`, `GitModeRunner`, `GitResumeContinuation`: unwrap with `orElseThrow(() -> new InternalErrorException("task \"<id>\": <path> absent at HEAD of <worktree> although this run committed it"))` — the application's existing exception for an impossible recorded state (design D2); `BranchStateFileMissingException` is an `adapters/git` type `application` has no compile edge to and MUST NOT be referenced; on these paths the envelope was committed by the same run, so absence is an invariant violation; verify `TakeFreshClaimSpec`, `GitResumeRoutingSpec`, `GitModeRunnerFreshRunSpec`, `RecordedStateReadbackSpec` pass and `grep -rn "BranchStateFileMissingException" application/src/main` is empty (FR1)
- [x] 3.3 Update the specs that pinned the old failure shape — `GitTaskStoreSpec` "reading a state file that is not there fails loudly", the take command's exception-mapping specs (`TakeClaimAndWorkSpec`, `TakeFenceScopeSpec`, `TakeResumeReplicationSpec`, `TakeSummaryAnchorSpec`, `TakeRepairObservabilitySpec`) and `BaseRefBareRemoteIntegrationSpec` — to the `Optional` protocol; verify `:application:check` and `:bootstrap:test` pass (FR1, FR5)

## 4. Convergence on the real medium (FR7, NFR-R1, NFR-C1)

- [x] 4.1 Verify 1.2 is green: the new `FinishKillPoints` row converges on the same worktree, the worktree directory is gone afterwards, the second pass changes no tip and posts no second finish, and the engine is never entered (the world's executor fake counts zero rounds); run `TransitionKillPointSpec` twice in a row (FR7, NFR-R1, NFR-C1, M1)
- [x] 4.2 Add to `TakeResumeShapeTailSpec` (the spec that drives the `CompletedUncleaned` tail through `TakeLoadedBranchRoutes` over a stubbed store) the assertion that a `CompletedUncleaned` tip whose stubbed `readTaskRecord` answers a present `Completed` record routes to `finishUncleaned`, never `deliverCompleted`, and that the delivered report's final state equals the stubbed `readRecordedState` answer; verify green (FR1, FR5)
- [x] 4.3 NFR-P1 on the pickup: in the 1.2 row, build the host world's `GitTaskRepository` over a recording git stand-in (`BaseRefreshCloneSafetySpec.recordingGit`) and assert the `CompletedUncleaned` pickup issues at most ten `show`/`cat-file -e` invocations against the worktree's `HEAD` and no `Files.` read of an envelope path (the gate of 5.1 proves the latter statically; the count is the dynamic half); verify green and record the count in the task report (NFR-P1, M9)

## 5. Enforcement (FR8, G3, M2)

- [x] 5.1 Add `EnvelopeMediumBoundarySpec` to `:bootstrap` (`architecture` package, over `RepoSourceTree.productionSources` with its comment-stripping helper, so javadoc mentions are never hits): scan `adapters/git/src/main` and `application/src/main/java/com/github/oinsio/gnomish/app` for the ten `Files.` read calls of design D6 (`readString`, `exists`, `notExists`, `isRegularFile`, `isDirectory`, `readAllBytes`, `newBufferedReader`, `lines`, `list`, `walk`); allowlist by path with reason — `TaskWorktreeManager` (`isDirectory` on the worktree path before registration), `DirectoryWorkspace` (`isDirectory` on the workspace root), `WorktreeJanitor` (`list`/`walk` over workspace roots), `AdHocTaskSynthesizer` (`readString` of an operator task file) — assert every allowlisted file was reached (each of the four contains at least one of the ten calls today: `TaskWorktreeManager:83`, `DirectoryWorkspace:31`, `WorktreeJanitor:128,200`, `AdHocTaskSynthesizer:102`), and make the failure message name the offending file, the call, and `GitTaskStore`/`GitShowTip` as the owner; add a second rule over every production tree (`*/src/main`, `adapters/*/src/main`, `sandbox/*/src/main`) rejecting the string literals `".gnomish-task"`, `"task.json"`, `"state.json"` outside `domain/.../branch/EnvelopePaths.java` (FR9, design D8), with the failure naming `EnvelopePaths` as the owner; verify it passes after groups 2–3 and 8, and fails with the expected message when a `Files.readString` of `EnvelopePaths.TASK_JSON_PATH` is temporarily added to `GitTaskStore` (FR8, M2)
- [x] 5.2 Run the M2 and M6 greps from the proposal and reconcile every hit against the allowlist and the owner; record the lists in the task report (M2, M6)
- [x] 5.3 Keep the UX2 message check: hold the tokens, the scan and the two failure texts in `EnvelopeMediumRule` (`:bootstrap`, `architecture`), and add `EnvelopeMediumDetectorSpec` driving it over seeded violating lines — a worktree `Files.readString` and a retyped `".gnomish-task"` — asserting each is reported as `path:line — token` and that the composed failure names the path, the line, the token and the owner (`GitTaskStore`/`GitShowTip`, `EnvelopePaths`), plus that a commented mention is never a hit; verify the two UX2 scenarios go red when the hint or the line number is removed from the message (UX2, FR8, FR9)
- [x] 5.4 Same check on the two sibling gates the design names as precedents (archived changes, so the record lands here): `ClaimlessGitBoundarySpec` — hold its three tokens and its scan in `ClaimlessGitRule`, make the reach check require the token rather than only the path (which surfaced three stale exemptions: `ContainerRunSupportFactory` and `ManualRunRunner`, which take a `ClaimEpochSource` parameter now and no longer choose, and `UsageCommandSpec`, which asks `TaskGitFixture.realClaimless()` by name), drop those rows with the reason in the javadoc, and add `ClaimlessGitDetectorSpec` over seeded sources; `BaseHeadDefaultBoundarySpec` — extract `spellsHeadDefault` and add the seeded table (a bare default and a default in a call are found, a javadoc mention and a trailing comment are not); verify each detector goes red when it is broken on purpose (FR8, M2)

## 6. Durable record (G1, M3)

- [x] 6.1 `docs/adr/0003-crash-consistency.md`: in "Atomicity and durability per medium", add the sentence that every host-side read of an envelope file resolves at the worktree's `HEAD` and that the working tree is the staging area for the next write, never a read medium; name this change as provenance; verify the table still renders (G1)
- [x] 6.2 `.claude/rules/crash-consistency.md`: add checklist item 11, "**Readers name their medium.** Every decision on a recovery path reads the durable medium (tip, tracker); a local working copy is a write staging area, and a read of a factory-owned file from it is a defect the `EnvelopeMediumBoundarySpec` gate rejects"; verify the item is under six lines and cites the gate by name (G3)
- [x] 6.3 Marker text of the three read pairs (all marker-declared on both ends today — `CleanupCommit:37` ↔ `GitObjectsTerminalCommits:102`, `TerminalWriteMarker:52` ↔ `GitObjectsTerminalCommits:57`, `StateFileWrite:93` ↔ `TaskLifecycleCommitWriter:128` — so `manual-sync-pairs.md` gains no row): rewrite each end's `Kept in sync with` sentence to the tip-only invariant of the design's Sync-surfaces table ("both test the tip", "both read the DTO they rewrite from the tip", "both read the cursor from the tip's state.json") and delete the "the media differ" clause from `StateFileWrite` and `TaskLifecycleCommitWriter:130`; verify `grep -rn "Kept in sync with" adapters/git/src/main` shows all six ends with the updated invariant and `grep -rn "media differ" adapters/git/src/main` is empty (M3)
- [x] 6.4 `docs/glossary.md`, Crash consistency section: add **Envelope** — "the factory-owned files under `.gnomish-task/` at a branch tip that record a task's identity (`task.json`) and pipeline position (`state.json`); read from the tip in every medium, written to a working copy only as the staging step of the next commit. *Never:* 'state directory' for the concept (that names the path), 'worktree state' (a working copy holds no envelope, only a staged copy of one)"; verify the term is not already defined elsewhere and no banned synonym appears in the diffs of this change (G1)
- [x] 6.5 Sweep javadoc: `GitTaskStore` (no longer "reads the worktree's files directly"), `HostResumeMechanics` (no longer "read from that worktree"), `TakeResumeBootstrap`, `GitResumeRunner`, `TaskStoreGit`'s port javadoc, `StateFileWrite`'s kill-window paragraph ("the only reader of the worktree's state file before the next commit" — there is none now); verify `grep -rn "from the worktree\|worktree's task.json\|worktree's state.json" --include='*.java' adapters/git/src/main application/src/main` returns only writes (G1)

## 8. One owner for the envelope's path names (FR9, G4, M6)

- [x] 8.1 Add `domain.branch.EnvelopePaths` (final class, private constructor): `DIR_NAME = ".gnomish-task"`, `DIR = DIR_NAME + "/"`, `TASK_FILE = "task.json"`, `STATE_FILE = "state.json"`, `TASK_JSON_PATH`, `STATE_JSON_PATH`, `DECISIONS_DIR = DIR + "decisions"`, with javadoc naming it the one spelling for every module and citing design D8; make `BranchShapeClassifier.TASK_FILE` / `STATE_FILE` aliases of it; add `EnvelopePathsSpec` pinning each constant's value and the derivations; verify `:domain:check` including PIT passes (FR9)
- [x] 8.2 Re-point every `GnomishTaskPaths.*` reference in `adapters/git/src/main` (33 on 2026-09-21, plus `FactoryOwnedPaths`'s two derivations) and in `adapters/git/src/test` to `EnvelopePaths`, then delete `GnomishTaskPaths.java`; also re-point the five bare-literal sites that never went through it — `state/TaskJsonMapper:63` and `state/PinnedRefGate:37` (`"task.json"` → `EnvelopePaths.TASK_FILE`), `state/StateJsonMapper:59` (`"state.json"` → `EnvelopePaths.STATE_FILE`), `HarvestedBoundaryCheck:99` and `RoundBoundaryCheck:151` (`".gnomish-task/ was modified…"` → `EnvelopePaths.DIR + " was modified…"`); verify `:adapters:git:check` passes, `grep -rn "GnomishTaskPaths" --include='*.java' --include='*.groovy' .` outside `openspec/changes/archive` is empty, and the M6 grep over `adapters/git/src/main` hits nothing (FR9, M6)
- [x] 8.3 `ContainerTipReader` (`:bootstrap`): replace the two hand-typed paths with `EnvelopePaths.STATE_JSON_PATH` / `TASK_JSON_PATH`; verify `ContainerRunSupportSpec` and the container tip-reader specs pass and 5.1's literal rule is green with no allowlist entry for `ContainerTipReader` (FR9, M6)
- [x] 8.4 `.claude/rules/manual-sync-pairs.md`: record under "Initial registry" that the `GnomishTaskPaths`/`BranchShapeClassifier` file-name pair never had a row because it was dissolved by `fix-envelope-medium` into one owner — one sentence in the paragraph above the table, not a row; verify no `Kept in sync with` marker mentions `GnomishTaskPaths` (FR9)

## 9. The stderr excerpt bounds its output (FR10, G5, M7)

- [x] 9.1 Add to `UntrustedTextSpec` the data-driven scenario "excerpt(cap) never exceeds cap after rendering": raw texts of 1 400 × `U+2028`, 1 400 × `U+2029`, a mixed text, and plain ASCII; assert `excerpt(1_400).length() <= 1_400` and that the result ends with the tail of the rendered text and carries the truncation marker when cut; add to `GitCommandResultDetailSpec` (the spec that pins `failureDetail`) the M7 scenario: a result whose stderr is 1 400 × `U+2028`, `failureDetail("fetch").forLog()` contains `the fetch exited 128`; verify both are red today — the first returns ~8 400 characters, the second loses its head (FR10, M7)
- [x] 9.2 Change `UntrustedText.excerpt` per design D9 (a second `capTail` applied after `flatten`, the same marker), leaving `forLog()` untouched; rewrite the `STDERR_CAP_CHARS` javadoc in `GitCommandResult` to state the bound in rendered characters and that the headroom under `DEFAULT_CAP_CHARS` holds for any stderr content; verify 9.1 is green and `:untrustedtext:check` including PIT passes with no new exemption (FR10)
- [x] 9.3 Confirm the sink idempotence property still holds for excerpt output: run the existing real-appender corpus spec of `factory-logging` (`OperatorLogIsolationSpec` / the sink corpus spec) and record the result (NFR-S1)

## 10. `whitelist` becomes `allowlist` (FR11, M8)

- [x] 10.1 Rename `HttpCheckVariables.WHITELIST` → `ALLOWLIST`, `HttpCheckVariableException(String, boolean whitelisted)` → `allowlisted`, and reword every javadoc and comment in `HttpCheckParamsValidator`, `HttpCheckVariableException`, `HttpCheckRequest`, `HttpCheckClientFactory`, `HttpCheckVariables`, `HttpExternalCheckClient`, `StalenessMemory`, `NonAtomicWrite`, `RunCheckRunContext`, `ProviderDispatchingExternalCheckClient`, `CheckRunContext`, `CheckClientFactory`, and the specs `HttpCheckParamsValidatorSpec`, `HttpCheckVariablesSpec`, `HttpExternalCheckClientRequestSpec`, `RunCheckRunContextSpec`, `AtomicWriteBoundarySpec`, `CheckRunContextSpec`; verify `:adapters:check`, `:bootstrap:test`, `:gnomish-plugin-api:check` (including `japicmpApiGate`) pass with no version bump (FR11)
- [x] 10.2 Verify the M8 grep (`grep -rin whitelist --include='*.java' --include='*.groovy' --include='*.md' .` outside `openspec/`) returns only the glossary's *Never:* line. Do NOT edit `openspec/specs/factory-egress-allowlist/spec.md` (a stable spec; its requirement heading "…restricted to a whitelist" is renamed by a delta of that capability in the next change that touches it) or `openspec/changes/add-subprocess-access-log/{design,tasks}.md` (another active change's artifacts) — list both as follow-ups in the task report (M8, NG8)

## 7. Verification and report (M1–M8)

- [x] 7.1 Run the root `check` (all modules, PIT under each module's gate, `checkTestTimeInjection`, Spotless, dependency analysis) and record the result; verify no new `@DoNotMutate` or `excludedClasses` entry was added
- [x] 7.2 Old-way sweep per `.claude/rules/implementation.md`: `grep -rn "Files\.readString\|Files\.exists\|Files\.isDirectory\|NoSuchFileException\|tip + \"\\^\"\|cat-file\|GnomishTaskPaths\|\"\.gnomish-task\|\"task\.json\"\|\"state\.json\"\|whitelist" --include='*.java' */src/main adapters/*/src/main sandbox/*/src/main` — reconcile every hit against the single-owner table in design.md (owner, listed consumer, or the named exemptions: `HarvestedStateCommitCheck:76` for `tip + "^"`; `TaskBranchCreator`, `EnvironmentSalvage`, `FactoryCloneHardening` for `cat-file`; `PathSafety` for `NoSuchFileException`; the D6 allowlist for `Files.*`) and list the hits with their disposition in the task report
- [x] 7.3 Record in the task report the run in which 1.1–1.4 and 9.1 were red and the run in which they went green (M4, M5, M7), the M9 counts from 2.2 and 4.3, the two NG8 follow-ups from 10.2, and recommend the commit message (subject ≤ 72 chars, trailer with the change name and FR IDs) — the human commits
