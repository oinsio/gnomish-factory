# Tasks: add-project-skeleton

## 1. Build foundation

- [x] 1.1 Generate Gradle 9.x wrapper; create `settings.gradle` (with `foojay-resolver-convention` for JDK auto-provisioning), `gradle.properties` (configuration cache, build cache, parallel), `.gitignore`, `.editorconfig`, and `gradle/libs.versions.toml` with all dependency/plugin versions (FR1, NFR-R1, NFR-P1)
- [x] 1.2 Create `build.gradle` (Groovy DSL): java plugin, Java 25 toolchain, Spring Boot plugin + BOM, groovy plugin for tests, catalog-based dependencies (FR1)
- [x] 1.3 Add a trivial placeholder Spock spec and verify `./gradlew test` compiles Java 25 + Groovy and runs Spock (FR1, FR5) — toolchain-matrix proof before any production code

## 2. Quality gates

- [x] 2.1 Wire JaCoCo: XML + HTML reports generated by `check` (FR6, NFR-O1)
- [x] 2.2 Wire PIT: gradle-pitest-plugin + pitest-junit5-plugin, `targetClasses = com.github.oinsio.gnomish.*`, `mutationThreshold = 100`, bootstrap class excluded and the exception documented (FR6)
- [x] 2.3 Wire Spotless: palantir-java-format for Java, Groovy formatting for specs, check wired into `./gradlew check`; verify `spotlessApply` fixes a deliberately misformatted file (FR8)
- [x] 2.4 Auto-format layers: Claude Code PostToolUse hook in `.claude/settings.json` formatting edited `.java`/`.groovy` files via `spotlessApply -PspotlessIdeHook=<file>`; Gradle task installing a git pre-commit hook running `spotlessApply`; verify via a misformatted edit and by running the hook script directly — no commit (UX3)
- [x] 2.5 Wire Error Prone + NullAway: `net.ltgt.errorprone` plugin, JSpecify annotations, `AnnotatedPackages = com.github.oinsio.gnomish`, unused-code checks as errors; verify it bites — a deliberate null dereference and an unused private method each fail compilation (FR9)
- [x] 2.6 Wire the dependency-analysis plugin: fail on unused/misdeclared dependencies, hooked into `check`; verify with a deliberately unused dependency (FR10)
- [x] 2.7 Verify the gates bite: introduce a deliberate surviving mutant in the working tree (no commit), confirm `./gradlew check` fails naming the threshold, then revert it (G3, UX2, M3)

## 3. Walking skeleton (TDD)

- [x] 3.1 RED: Spock spec for `FactoryProperties` — valid values bind, blank/invalid `instance-id` throws with the property name in the message, record is immutable (FR3)
- [x] 3.2 GREEN: implement `FactoryProperties` record with compact-constructor validation under `com.github.oinsio.gnomish` (FR3)
- [x] 3.3 RED: `spock-spring` spec — context boots with `application.yaml`, properties bean is populated, the runtime is a headless outbound-only worker; environment variable overrides the YAML value (FR2, FR3)
- [x] 3.4 GREEN: `FactoryApplication` (`@SpringBootApplication`, `@ConfigurationPropertiesScan`, `main()`) + minimal `application.yaml` (FR2)
- [x] 3.5 Logging: log level configurable via configuration/environment without recompilation; verify DEBUG appears when enabled (FR4)
- [x] 3.6 Run full `./gradlew check`: Spock + JaCoCo + PIT green, mutation score 100% (FR5, FR6, M2)

## 4. Continuous integration

- [x] 4.1 GitHub Actions workflow: JDK 25 (Temurin), Gradle action with wrapper validation, `./gradlew check` on push/PR, JaCoCo + PIT reports uploaded as artifacts including on failure; least-privilege `permissions` and `concurrency` with stale-run cancellation (FR7, NFR-O1, NFR-C1, NFR-S2)
- [x] 4.2 Dependabot config: `gradle` and `github-actions` ecosystems, weekly schedule (NFR-R2)
- [x] 4.3 Security workflows: CodeQL (java), OSV-Scanner as a failing gate on vulnerable dependency versions, Gitleaks secret scan; note to enable GitHub push protection in repo settings after the remote is created (FR11, NFR-S1)

## 5. Verification and docs

- [x] 5.1 Fresh-clone check: clean clone into a temp dir, `./gradlew check` passes on JDK 25 with no manual setup (G1, M1, NFR-P1)
- [x] 5.2 Update README: build prerequisites (JDK 25, Docker note for future E2E) and the `./gradlew check` command (UX1)
- [x] 5.3 Grep-verify traceability: every FR/NFR/UX of the proposal has at least one implementing artifact or documented exception (process rule)
