# Tasks: fix-envelope-medium

> Sequencing: starts after `type-untrusted-text` is committed and archived (proposal, header).
> Group 1 lands the red specs first — the report's reproduction (1.1) must be red before group 2
> touches the readers, and the task report records the run in which it was red. Groups 2–4 are
> the reader move; group 5 the gate; group 6 the durable record; groups 8–10 the three folded-in
> findings (path owner, excerpt bound, rename) — 8 must precede 5.1's literal rule, 9 and 10 are
> independent of everything else. Each task's sub-agent report ends with the old-way sweep it ran
> (`.claude/rules/implementation.md`).

## 1. Red specs on the real medium (FR3, FR6, FR7, M4, M5)

- [ ] 1.1 Add to `GitTaskRepositorySpec` the scenario "finishCleanup converges a removal a killed predecessor already staged": create and complete a task on a real clone, run `git rm -r .gnomish-task` in its worktree by hand without committing, call `finishCleanup`, and assert the tip's tree lacks `.gnomish-task/` and a second `finishCleanup` changes the tip's id no further; verify it is red today (the guard reads the worktree and skips) and the failure names the tip still carrying the directory (FR3, M4)
- [ ] 1.2 Add to `FinishKillPoints` a fourth durable step for the host medium only — "the staged removal", `git rm -r .gnomish-task` in the world's worktree, no commit — declared so the container world's row keeps three steps (`KillPointWorlds` decides per medium); frozen shape `CompletedUncleaned`, pickup through the existing `FinishEffect` + `finishCleanup` on the same world and the same worktree, converged `Delivered`, worktree removed, second pass a no-op; verify `TransitionKillPointSpec` is red on the new row today (FR7, NFR-R1, M1)
- [ ] 1.3 Add to `DeliveredBranchReaderSpec` two scenarios: "reads the delivered state from the Completed commit when the branch gained commits after cleanup" (append one plain commit after the cleanup commit, then `read`) and "reads at the tip when the tip still carries the envelope" (a `CompletedUncleaned` branch); verify both are red today — the first reads the wrong revision, the second reads `tip^` (FR6, M5)
- [ ] 1.4 Add to `TakeResumeRoutingSpec` (the spec that drives `HostResumeMechanics` over a real clone) the scenario "a worktree without the envelope over a tip that carries it loads the branch": stage the removal by hand, call `loadBranch`, assert a non-null bundle with the `Completed` outcome; verify red today (`null`, the delivered route) (FR1, FR5)

## 2. The tip reader serves the host medium (FR1, FR2, FR4, NFR-R2)

- [ ] 2.1 Add `GitShowTip.carries(String path)` — `git cat-file -e <revision>:<path>` through `GitReadGate.answered`, true on exit 0 — and `GitShowTip.cleanupCommit()` returning `Optional<String>` (the located id from the existing `rev-list` walk), keeping `cleanupCommitInHistory()` as its `isPresent()` wrapper; extend `BranchTipSourceSpec` (the read answers) and `GitShowTipTerminationSpec` (the cut-off invocation) so a cut-off `cat-file` throws `BranchTipUnavailableException` rather than answering false; verify `:adapters:git:test` passes (FR4, FR6, NFR-R2)
- [ ] 2.2 Change `TaskStoreGit.readRecordedState` / `readTaskRecord` to return `Optional<TaskState>` / `Optional<TaskRecord>` and reimplement both in `GitTaskStore` over `new GitShowTip(runner, worktree, GitObjects.HEAD)`'s `readAtTip`, deleting `GitTaskStore.read` and its `Files.readString`; rewrite the four `GitTaskStoreSpec` read scenarios over a real clone (present at the tip; absent at the tip → empty; worktree file differs from the tip → the tip's value wins; a cut-off `git show` → `BranchTipUnavailableException`); verify `:adapters:git:test` passes and `grep -n "Files\." adapters/git/src/main/java/com/github/oinsio/gnomish/adapter/git/GitTaskStore.java` is empty (FR1, NFR-R2)
- [ ] 2.3 Route the three lifecycle rewrites through the tip: `TerminalWriteMarker.clearPending` and `GitTaskRepository.readCurrentDto` read the DTO from `GitShowTip.readAtTip(TASK_JSON_PATH)` at `HEAD` (absent → `GitTaskRepositoryException` as today's I/O failure did), `StateFileWrite.currentCursor` reads the cursor from `readAtTip(STATE_JSON_PATH)` and degrades to `null` as today; extend `GitTaskRepositorySpec` — the spec that drives `confirmTerminalWrite`, the outcome/decision rewrites, and the cursor carry-forward — with one scenario per site where the worktree file was altered by hand and the commit still carries the tip-derived value; verify `:adapters:git:check` including PIT passes with no new exemption (FR2)
- [ ] 2.4 `CleanupCommit.commit`: guard on `GitShowTip.carries(DIR_NAME)` at `HEAD`; run `git rm -r --ignore-unmatch <DIR_NAME>`; when `rm` reports nothing removed while the guard held, log the INFO line of NFR-O1 (`cleanup commit for task {} lands a removal a predecessor already staged`); commit as today; rewrite the class javadoc's kill-window paragraph to the four-row matrix of design D4 and the "Kept in sync with" marker to "both test the tip"; verify 1.1 is green and `GitTaskRepositorySpec`'s existing "finishCleanup on an already-cleaned tip changes nothing" still passes (FR3, NFR-O1)
- [ ] 2.5 `WorktreeSalvage.restoreFactoryFiles`: replace the inline `cat-file -e HEAD:<STATE_DIR>` with `GitShowTip.carries` at `HEAD`, adjusting the "Kept in sync with" marker text; verify `WorktreeSalvageSpec` passes and `grep -rn "cat-file" adapters/git/src/main` hits only `GitShowTip` (FR4)
- [ ] 2.6 `DeliveredBranchReader`: resolve the revision as design D5 — the tip when `readAtTip(TASK_JSON_PATH)` at the tip is present, else `cleanupCommit() + "^"`, else `BranchStateFileMissingException` — and delete the `tip + "^"` literal and the two javadoc paragraphs recording the task 6.5 concern; verify 1.3 is green and the existing four `DeliveredBranchReaderSpec` scenarios still pass (FR6, M5)

## 3. Application call sites carry typed absence (FR1, FR5)

- [ ] 3.1 `HostResumeMechanics`: `loadBranch` returns `null` on an empty `readTaskRecord` (the delivered route), `readFinalState` returns the first-stage state on an empty `readRecordedState` (the pre-contract tip); delete both `catch (UncheckedIOException)` arms and the `NoSuchFileException` import; verify 1.4 is green and `TakeResumeRoutingSpec`, `TakeResumeShapeTailSpec` pass (FR5)
- [ ] 3.2 `TakeResumeBootstrap` (and through it `GitResumeRunner`), `TakeFreshClaim`, `GitResumeRunner.continueFrom`, `GitModeRunner`, `GitResumeContinuation`: unwrap with `orElseThrow(() -> new BranchStateFileMissingException(GitObjects.HEAD, <path>, …))` — on these paths the envelope was committed by the same run, so absence is a fault; verify `TakeFreshClaimSpec`, `GitResumeRoutingSpec`, `GitModeRunnerFreshRunSpec`, `RecordedStateReadbackSpec` pass (FR1)
- [ ] 3.3 Update the specs that pinned the old failure shape — `GitTaskStoreSpec` "reading a state file that is not there fails loudly", the take command's exception-mapping specs (`TakeClaimAndWorkSpec`, `TakeFenceScopeSpec`, `TakeResumeReplicationSpec`, `TakeSummaryAnchorSpec`, `TakeRepairObservabilitySpec`) and `BaseRefBareRemoteIntegrationSpec` — to the `Optional` protocol; verify `:application:check` and `:bootstrap:test` pass (FR1, FR5)

## 4. Convergence on the real medium (FR7, NFR-R1, NFR-C1)

- [ ] 4.1 Verify 1.2 is green: the new `FinishKillPoints` row converges on the same worktree, the worktree directory is gone afterwards, the second pass changes no tip and posts no second finish, and the engine is never entered (the world's executor fake counts zero rounds); run `TransitionKillPointSpec` twice in a row (FR7, NFR-R1, NFR-C1, M1)
- [ ] 4.2 Add to `TakeResumeShapeTailSpec` (the spec that drives the `CompletedUncleaned` tail through `TakeLoadedBranchRoutes`) the assertion that a `CompletedUncleaned` tip over a stale worktree routes to `finishUncleaned`, never `deliverCompleted`, and that the delivered report's final state equals the tip's `state.json` position; verify green (FR1, FR5)

## 5. Enforcement (FR8, G3, M2)

- [ ] 5.1 Add `EnvelopeMediumBoundarySpec` to `:bootstrap` (`architecture` package, over `RepoSourceTree.productionSources`): scan `adapters/git/src/main` and `application/src/main/java/com/github/oinsio/gnomish/app` for the nine `Files.` read calls of design D6; allowlist by path with reason — `TaskWorktreeManager` (worktree directory registration), `DirectoryWorkspace` and `WorktreeJanitor` (workspace roots), `AdHocTaskSynthesizer` (operator task file) — assert every allowlisted file was reached, and make the failure message name the offending file, the call, and `GitTaskStore`/`GitShowTip` as the owner; add a second rule over every production tree (`*/src/main`, `adapters/*/src/main`, `sandbox/*/src/main`) rejecting the string literals `".gnomish-task"`, `"task.json"`, `"state.json"` outside `domain/.../branch/EnvelopePaths.java` (FR9, design D8), with the failure naming `EnvelopePaths` as the owner; verify it passes after groups 2–3 and fails with the expected message when a `Files.readString` of `GnomishTaskPaths.TASK_JSON_PATH` is temporarily added to `GitTaskStore` (FR8, M2)
- [ ] 5.2 Run the M2 and M6 greps from the proposal and reconcile every hit against the allowlist and the owner; record the lists in the task report (M2, M6)

## 6. Durable record (G1, M3)

- [ ] 6.1 `docs/adr/0003-crash-consistency.md`: in "Atomicity and durability per medium", add the sentence that every host-side read of an envelope file resolves at the worktree's `HEAD` and that the working tree is the staging area for the next write, never a read medium; name this change as provenance; verify the table still renders (G1)
- [ ] 6.2 `.claude/rules/crash-consistency.md`: add checklist item 11, "**Readers name their medium.** Every decision on a recovery path reads the durable medium (tip, tracker); a local working copy is a write staging area, and a read of a factory-owned file from it is a defect the `EnvelopeMediumBoundarySpec` gate rejects"; verify the item is under six lines and cites the gate by name (G3)
- [ ] 6.3 `.claude/rules/manual-sync-pairs.md`: rewrite the `GitAttemptPersistence`/`EnvironmentAttemptPersistence` row's neighbour rows that this change touches — add the three pairs of design D3/D4 (`CleanupCommit`↔`GitObjectsTerminalCommits.cleanUp`, `TerminalWriteMarker`↔`clearPending`, `StateFileWrite.currentCursor`↔`tipStateCursor`) only if they are not already carried by both ends' markers; where both ends carry the marker, confirm the marker text says "the tip" and delete the "the media differ" clause from `StateFileWrite` and `TaskLifecycleCommitWriter`; verify `grep -rn "Kept in sync with" adapters/git/src/main` shows every touched end with the updated invariant (M3)
- [ ] 6.4 `docs/glossary.md`, Crash consistency section: add **Envelope** — "the factory-owned files under `.gnomish-task/` at a branch tip that record a task's identity (`task.json`) and pipeline position (`state.json`); read from the tip in every medium, written to a working copy only as the staging step of the next commit. *Never:* 'state directory' for the concept (that names the path), 'worktree state' (a working copy holds no envelope, only a staged copy of one)"; verify the term is not already defined elsewhere and no banned synonym appears in the diffs of this change (G1)
- [ ] 6.5 Sweep javadoc: `GitTaskStore` (no longer "reads the worktree's files directly"), `HostResumeMechanics` (no longer "read from that worktree"), `TakeResumeBootstrap`, `GitResumeRunner`, `TaskStoreGit`'s port javadoc, `StateFileWrite`'s kill-window paragraph ("the only reader of the worktree's state file before the next commit" — there is none now); verify `grep -rn "from the worktree\|worktree's task.json\|worktree's state.json" --include='*.java' adapters/git/src/main application/src/main` returns only writes (G1)

## 8. One owner for the envelope's path names (FR9, G4, M6)

- [ ] 8.1 Add `domain.branch.EnvelopePaths` (final class, private constructor): `DIR_NAME = ".gnomish-task"`, `DIR = DIR_NAME + "/"`, `TASK_FILE = "task.json"`, `STATE_FILE = "state.json"`, `TASK_JSON_PATH`, `STATE_JSON_PATH`, `DECISIONS_DIR = DIR + "decisions"`, with javadoc naming it the one spelling for every module and citing design D8; make `BranchShapeClassifier.TASK_FILE` / `STATE_FILE` aliases of it; add `EnvelopePathsSpec` pinning each constant's value and the derivations; verify `:domain:check` including PIT passes (FR9)
- [ ] 8.2 Re-point every `GnomishTaskPaths.*` reference in `adapters/git/src/main` (33 on 2026-09-21, plus `FactoryOwnedPaths`'s two derivations) and in `adapters/git/src/test` to `EnvelopePaths`, then delete `GnomishTaskPaths.java`; verify `:adapters:git:check` passes and `grep -rn "GnomishTaskPaths" --include='*.java' --include='*.groovy' .` outside `openspec/changes/archive` is empty (FR9)
- [ ] 8.3 `ContainerTipReader` (`:bootstrap`): replace the two hand-typed paths with `EnvelopePaths.STATE_JSON_PATH` / `TASK_JSON_PATH`; verify `ContainerRunSupportSpec` and the container tip-reader specs pass and 5.1's literal rule is green with no allowlist entry for `ContainerTipReader` (FR9, M6)
- [ ] 8.4 `.claude/rules/manual-sync-pairs.md`: record under "Initial registry" that the `GnomishTaskPaths`/`BranchShapeClassifier` file-name pair never had a row because it was dissolved by `fix-envelope-medium` into one owner — one sentence in the paragraph above the table, not a row; verify no `Kept in sync with` marker mentions `GnomishTaskPaths` (FR9)

## 9. The stderr excerpt bounds its output (FR10, G5, M7)

- [ ] 9.1 Add to `UntrustedTextSpec` the data-driven scenario "excerpt(cap) never exceeds cap after rendering": raw texts of 1 400 × `U+2028`, 1 400 × `U+2029`, a mixed text, and plain ASCII; assert `excerpt(1_400).length() <= 1_400` and that the result ends with the tail of the rendered text and carries the truncation marker when cut; add to `GitCommandResultSpec` (or the spec that pins `failureDetail`) the M7 scenario: a result whose stderr is 1 400 × `U+2028`, `failureDetail("fetch").forLog()` contains `the fetch exited 128`; verify both are red today — the first returns ~8 400 characters, the second loses its head (FR10, M7)
- [ ] 9.2 Change `UntrustedText.excerpt` per design D9 (a second `capTail` applied after `flatten`, the same marker), leaving `forLog()` untouched; rewrite the `STDERR_CAP_CHARS` javadoc in `GitCommandResult` to state the bound in rendered characters and that the headroom under `DEFAULT_CAP_CHARS` holds for any stderr content; verify 9.1 is green and `:untrustedtext:check` including PIT passes with no new exemption (FR10)
- [ ] 9.3 Confirm the sink idempotence property still holds for excerpt output: run the existing real-appender corpus spec of `factory-logging` (`OperatorLogIsolationSpec` / the sink corpus spec) and record the result (NFR-S1)

## 10. `whitelist` becomes `allowlist` (FR11, M8)

- [ ] 10.1 Rename `HttpCheckVariables.WHITELIST` → `ALLOWLIST`, `HttpCheckVariableException(String, boolean whitelisted)` → `allowlisted`, and reword every javadoc and comment in `HttpCheckParamsValidator`, `HttpCheckVariableException`, `HttpCheckRequest`, `HttpCheckClientFactory`, `HttpCheckVariables`, `HttpExternalCheckClient`, `StalenessMemory`, `NonAtomicWrite`, `RunCheckRunContext`, `ProviderDispatchingExternalCheckClient`, `CheckRunContext`, `CheckClientFactory`, and the specs `HttpCheckParamsValidatorSpec`, `HttpCheckVariablesSpec`, `HttpExternalCheckClientRequestSpec`, `RunCheckRunContextSpec`, `AtomicWriteBoundarySpec`, `CheckRunContextSpec`; verify `:adapters:check`, `:bootstrap:test`, `:gnomish-plugin-api:check` (including `japicmpApiGate`) pass with no version bump (FR11)
- [ ] 10.2 Reword the two active-change artifacts that use the word (`openspec/changes/add-subprocess-access-log/design.md`, `tasks.md`) and check `openspec/specs/factory-egress-allowlist/spec.md`'s seven occurrences are the *Never:*-style mentions the glossary permits or reword them; verify the M8 grep returns only the glossary's *Never:* line (M8)

## 7. Verification and report (M1–M8)

- [ ] 7.1 Run the root `check` (all modules, PIT under each module's gate, `checkTestTimeInjection`, Spotless, dependency analysis) and record the result; verify no new `@DoNotMutate` or `excludedClasses` entry was added
- [ ] 7.2 Old-way sweep per `.claude/rules/implementation.md`: `grep -rn "Files\.readString\|Files\.exists\|NoSuchFileException\|tip + \"\\^\"\|cat-file\|GnomishTaskPaths\|\"\.gnomish-task\|whitelist" --include='*.java' */src/main adapters/*/src/main sandbox/*/src/main` — reconcile every hit against the single-owner table in design.md (owner, listed consumer, or allowlisted exemption) and list the hits with their disposition in the task report
- [ ] 7.3 Record in the task report the run in which 1.1–1.4 and 9.1 were red and the run in which they went green (M4, M5, M7), and recommend the commit message (subject ≤ 72 chars, trailer with the change name and FR IDs) — the human commits
