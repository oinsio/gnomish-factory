purpose: plan the work from the task description
inputs:
  - kind: source
outputs:
  - id: plan-doc
executor:
  type: agent-cli
  model: plan-model
  settings:
    maxTurns: 10
instructions: stages/plan/instructions.md
verify:
  - type: builtin
    name: files_exist
    params:
      paths:
        - plan.md
advancement: auto
autonomy:
  attemptLimit: 5
