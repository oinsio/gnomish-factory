// Build script for the gnomish-factory root project.
//
// The root project holds no source: since `:bootstrap` was carved out of the
// remainder (task 4.7 of split-into-modules), every production class and spec
// lives in a module and the root is build-wide metadata only — the formatting
// gate over the build files (via `build-metadata-conventions`), the
// dependency-analysis gate, and the git-hook installer. Everything a module
// needs comes from the `build-logic` convention plugins (FR6, design D9).

// Dependency locking for the buildscript classpath (the convention plugins'
// own transitives). Together with the project-configuration locking from
// `java-conventions`, this produces the committed lock state OSV-Scanner reads
// in CI (FR11, design D9). `:bootstrap` carries the same block for the Boot
// Gradle plugin, which is applied there.
// The `buildscript` block must precede `plugins`, hence its position here.
buildscript {
    configurations.classpath {
        resolutionStrategy.activateDependencyLocking()
        // Security overrides forcing CVE fixes onto Gradle-plugin transitives.
        // Versions live in gradle.properties (the `buildscript*` keys), because
        // the version catalog is not visible to the buildscript classpath;
        // referencing them by name keeps version bumps out of this file.
        // `build-logic/build.gradle` reads the same keys for the plugin jars
        // that moved onto its classpath.
        // Every artifact forced below currently arrives through the Boot Gradle
        // plugin, i.e. on `:bootstrap`'s classpath rather than this one (see
        // that module's identical block, and this project's lockfile, which no
        // longer lists them). They are kept here as the standing guard for the
        // root's own plugin classpath: a convention-plugin transitive that
        // reintroduces one must land on the fixed version, not the vulnerable
        // one that made it a documented override.
        // commons-lang3: a plugin drags in 3.16.0 transitively (GHSA-j288-q9x7-2f5v).
        resolutionStrategy.force "org.apache.commons:commons-lang3:${buildscriptCommonsLang3Version}"
        // tools.jackson (Jackson 3.x): a plugin drags in 3.1.4; jackson-databind
        // 3.1.4 has GHSA-5gvw-p9qm-jgwh. Force the whole family so the
        // buildscript-classpath lock state stays consistent. Buildscript
        // classpath only — never reaches the application or test runtime.
        resolutionStrategy.force "tools.jackson.core:jackson-databind:${buildscriptJacksonVersion}",
            "tools.jackson.core:jackson-core:${buildscriptJacksonVersion}",
            "tools.jackson:jackson-bom:${buildscriptJacksonVersion}"
        // httpcore5: a plugin drags in 5.4 (GHSA-hf6x-8p5f-cgmf). h2 moves with it.
        resolutionStrategy.force "org.apache.httpcomponents.core5:httpcore5:${buildscriptHttpcore5Version}",
            "org.apache.httpcomponents.core5:httpcore5-h2:${buildscriptHttpcore5Version}"
        // httpclient5: a plugin drags in 5.6.1 (GHSA-hjcp-jmpx-g3qm); it is what
        // strictly requires the httpcore5 pair above, so the three move together.
        resolutionStrategy.force "org.apache.httpcomponents.client5:httpclient5:${buildscriptHttpclient5Version}"
    }
}

plugins {
    // `base` + the Spotless build-file format (FR6, design D9); the root
    // project applies no java/test convention because it has no sources.
    id 'build-metadata-conventions'
    // dependency-analysis is applied build-wide from settings.gradle (FR10,
    // design D8); only its configuration and `check` wiring live here.
}

// Dependency-analysis gate (FR10 of add-project-skeleton, design D8, dead-code
// layer 2 of 3): any unused or misdeclared dependency fails the build.
dependencyAnalysis {
    structure {
        // spring-boot-starter is a deliberate aggregate (design D3): the code
        // never compiles against the empty starter jar itself, it uses the
        // members the starter pulls in (Boot runtime, logging backend). The
        // bundle tells the analysis that using any member justifies declaring
        // the starter — DAGP's designed mechanism for starters, NOT an ignore:
        // if no member is used at all, the starter is still flagged as unused.
        bundle('spring-boot-starter') {
            primary 'org.springframework.boot:spring-boot-starter'
            includeGroup 'org.springframework.boot'
            includeDependency 'org.springframework:spring-core'
            includeDependency 'org.springframework:spring-context'
            includeGroup 'ch.qos.logback'
            includeGroup 'org.apache.logging.log4j'
            includeDependency 'io.micrometer:micrometer-observation'
        }
    }
    issues {
        all {
            onAny {
                severity 'fail'
            }
        }
        // `:gnomish-plugin-api:sample` is the executable statement of UX3: a
        // third-party adapter compiles against `gnomish-plugin-api` and NOTHING
        // else. DAGP's used-transitive rule would have it declare `:domain`
        // directly — the exact opposite of the promise the module exists to
        // prove. Narrowly scoped to that one rule in that one module; every
        // other check still fails the build there.
        project(':gnomish-plugin-api:sample') {
            onUsedTransitiveDependencies {
                severity 'ignore'
            }
        }
    }
}

// The dependency gate is part of the standard verification lifecycle (FR10).
// buildHealth is the plugin's aggregate verification task at the root project.
tasks.named('check') {
    dependsOn tasks.named('buildHealth')
}

// Opt-in whole-build mutation run (FR11, design D6, task 8.2 of
// split-into-modules). Since the split there is no whole-tree PIT task: every
// module mutates its own production packages and root `check` aggregates those
// runs. This task is the one-shot full report on demand — `./gradlew pitestAll`
// — without the rest of `check`, and is deliberately NOT wired into `check`,
// which would run each module's gate twice. Each module contributes itself from
// `pitest-conventions`; the reports stay per module (see README).
tasks.register('pitestAll') {
    description = 'Runs every module\'s PIT mutation gate; opt-in, not part of `check`.'
    group = 'verification'
}

// Formatting layer 2 of 3 (UX3, design D7): a git pre-commit hook running
// spotlessApply as a safety net. The hook source is versioned under
// config/git-hooks/; this task copies it into the git hooks dir with the exec
// bit. Wired into `check` so the hook is installed by the first build — no
// separate setup ritual (Copy task, configuration-cache compatible).
//
// Resolve the real hooks directory: in a plain clone `.git` is a directory, but
// in a git worktree `.git` is a file (`gitdir: <path>`) and the hooks live in
// the shared common git dir. Hardcoding `.git/hooks` breaks under a worktree.
def gitHooksDir = { ->
    def dotGit = layout.projectDirectory.file('.git').asFile
    if (dotGit.isDirectory()) {
        return new File(dotGit, 'hooks')
    }
    // Worktree: `.git` is a file pointing at the per-worktree gitdir; the
    // sibling `commondir` file (usually `../..`) locates the shared git dir.
    def gitDir = new File(dotGit.text.replaceFirst(/(?s)^gitdir:\s*/, '').trim())
    def commonDirMarker = new File(gitDir, 'commondir')
    def commonDir = commonDirMarker.isFile()
        ? new File(gitDir, commonDirMarker.text.trim()).canonicalFile
        : gitDir
    return new File(commonDir, 'hooks')
}()

tasks.register('installGitHooks', Copy) {
    description = 'Installs the versioned git pre-commit hook into the git hooks dir.'
    group = 'build setup'
    from layout.projectDirectory.file('config/git-hooks/pre-commit')
    into gitHooksDir
    filePermissions { unix(0755) }
}

tasks.named('check') {
    dependsOn tasks.named('installGitHooks')
}

// FR9 of add-functional-api-gate-test: `build-logic` is an INCLUDED build, so
// none of its verification runs unless the root asks for it — CI invokes
// `./gradlew check` and nothing else. Without this edge the TestKit suite that
// proves the api-compatibility gate still bites would only ever run when someone
// remembered `./gradlew -p build-logic check`, which is precisely the "verified
// once, manually" failure this change removes.
tasks.named('check') {
    dependsOn gradle.includedBuild('build-logic').task(':check')
}
