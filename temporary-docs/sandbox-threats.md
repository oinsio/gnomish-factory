# Реестр угроз песочницы

> Временный файл — не для коммита. Сводный список проблем безопасности,
> разобранных в explore-сессиях песочницы (2026-07-20…23, 2026-07-31).
> Заготовка под
> threat-model будущего sandbox-change. Объяснения простым языком — в
> `sandbox-explained.md`; технические решения и источники —
> в `explore-notes-sandbox.md`.
>
> Колонка «Закрывается» — каким механизмом; «Где» — change A (ядро песочницы:
> контейнер, клон/harvest, egress, env-allowlist) или change B (доводки:
> вскрытие TLS, virtual keys, setup-поверхность). Пометки: Д5/Д7 — решения
> нити Д factory-loop; «уже» — закрыто существующим дизайном/кодом.

## Файлы и доступ к диску

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 1 | Гном видит всё, к чему есть доступ у нас (личные файлы, чужие worktree, настройки фабрики) | контейнер: коробке видна только копия проекта | A |
| 2 | Обман текстом (prompt injection) → чтение и слив секретов | вся конструкция (в коробке нечего слить + egress по списку); принимается как данность | A |
| 3 | Запуск чужого кода при сборке/тестах (заражённая библиотека, кейс Nx «s1ngularity») | контейнер + лимиты + egress; изоляция от хоста | A |
| 4 | Ошибочное удаление/перезапись не того | контейнер: границы записи = только копия проекта | A |
| 5 | Подмена путей персистенции (git-хуки, `.gitconfig`, shell-rc, конфиг агента) → код исполнится позже вне коробки | read-only на эти пути внутри коробки; хуки не переезжают через `git fetch` | A |
| 6 | Туннель наружу через связанный worktree (общий `.git`) | клон-вместо-worktree: полная независимая копия в коробке | A |

## Секреты

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 7 | Наследование всего окружения (включая посторонние облачные ключи) | env по белому списку (в контейнере — пустое окружение по построению) | A |
| 8 | Утечка токена трекера в command-чеки (`./gradlew test`) | declared-scrub-list на CommandProcessRunner | Д5 (уже едет в add-claim-heartbeat) |
| 9 | Секрет в окружении агента будет выпрошен инъекцией (кейс Devin) | принцип «ни одного настоящего секрета в коробке» | A |
| 10 | Право push / push-креды в руках гнома | монополия push: гном коммитит внутри, фабрика пушит снаружи | уже (закрепляется A) |
| 11 | Секреты этапа установки, доживающие до фазы работы гнома | две фазы: секреты только в setup, стёрты до старта гнома | B |

## Сеть

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 12 | Эксфильтрация украденного на произвольный адрес | egress default-deny + allowlist (вахтёр) | A |
| 13 | Скачивание и запуск вредного кода из сети | тот же allowlist (только реестры/провайдер/git) | A |
| 14 | `HTTP_PROXY` — лишь конвенция, обходится прямым соединением | слой 2: у коробки нет маршрута наружу, кроме вахтёра | A |
| 15 | Разрешённый адрес — открытая дверь (эксфильтрация в чужой репо на github.com) | узкий список; L7-правила (github только чтение) | A (узкий) / B (L7) |
| 16 | Канал AI-провайдера — неустранимый путь; «чужой аккаунт» (allowlist = grant, не фильтр); инцидент Anthropic | вскрытие TLS: пин своего ключа, срез чужих auth-заголовков | B |
| 17 | Подмена конверта (domain fronting) без вскрытия TLS | вскрытие TLS на вахтёре (mitmproxy) | B |
| 18 | DNS как канал эксфильтрации (туннелирование) | DNS резолвит вахтёр, не коробка | A |
| 19 | Тихий отказ защиты (allowlist молча не сработал) | обязательная самопроверка egress при старте коробки | A |

## Ресурсы и соседи

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 20 | Исчерпание ресурсов хоста (CPU-цикл, утечка памяти, fork-бомба, диск) | лимиты контейнера (cgroups: cpus/memory/pids/диск) | A |
| 21 | Слабая взаимная изоляция параллельных задач (общее ядро одной VM на Mac) | контейнер-на-задачу; residual risk на Mac принят; Apple container/microVM — апгрейд | A (принят) / далее |

## Reward hacking и целостность правил

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 22 | Гном «сам себе ставит зачёт» (ослабляет критерии/тесты/лимиты) | «закон задачи» = `.gnomish/` из клона фабрики, гному недоступен | A |
| 23 | Репо-локальный конфиг (`.gnomish/`) как недоверенный вход, исполняемый до подтверждения доверия (инцидент Anthropic) | исполняется только внутри одноразовой коробки | A |

## Docker / вложенное исполнение

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 24 | Проброс docker.sock / CRI-сокета ноды в коробку = отдача хоста/ноды | сквозной запрет; вместо этого лесенка Docker-стратегии (ступени 0–3) | A/B |
| 25 | Privileged DinD-сайдкар снимает изоляцию (запрещён PodSecurity restricted) | сквозной запрет; sysbox/Kata/kubedock без privileged | по ступени |
| 26 | Секреты CI на гномьих ветках (workflow на push `gnomish/*` видит секреты репо) | workflow на `gnomish/*` без привилегированных секретов, GITHUB_TOKEN read-only | ступень 0 |

## Облако / k8s

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 27 | Metadata-сервис облака (`169.254.169.254`): SSRF → облачные креды аккаунта | обязательная блокировка metadata endpoint вахтёром/файрволом | cloud executor |
| 28 | Volume как второй источник правды → ломает stateless-фабрику | принцип «volume = только кэш тёплого resume, сносим без потери корректности» | cloud executor |
| 29 | Данные (код, промпты, история) уезжают в облако — точка доверия/юрисдикции | осознанный выбор оператора; для приватного репо на GitHub обычно приемлемо | cloud executor |
| 30 | Image pull делает нода/kubelet вне сетевой политики пода | registry-политика кластера | cloud executor |

## Доверие к инструментам

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 31 | Держатель ключа (шлюз) с плохой security-историей опасен сам по себе (пример OmniRoute: блокировка пакета, дефолтный админ-секрет, fail-open) | выбор зрелого локального шлюза (LiteLLM/Bifrost); критерий отбора в дизайне | B |

## Вердикты, findings и стоки данных (добавлено 2026-07-31)

| # | Угроза | Закрывается | Где |
|---|--------|-------------|-----|
| 32 | Findings любого чека (judge/external/command) как вектор инъекции: текст попадает в ретрай-промпт, трекер, логи | единая воронка findings: схема, ветвление только по passed/exit code, санитизация логов, fenced-публикация в трекере; ретрай-сток защищён коробкой по построению | A |
| 33 | Инъекция против самого судьи через артефакты («игнорируй критерии, ставь зачёт») | конкретные критерии, жёсткие разделители, N голосов для критичных стадий | A |
| 34 | Гном переписывает определение external-чека в своей ветке (workflow-файл, sonar-properties) → «сам себе зачёт» через CI до ревью | pin-чек ПЕРВЫМ: файлы определений побайтно сверяются с базовой веткой; серверные определения где можно | A |
| 35 | Подделка статуса external-чека токеном | поллить вердикты авторства платформы (conclusion workflow run), не check-run-статусы; в коробке нет токенов | A |
| 36 | Гигабайты findings/логов как ресурсная атака на фабрику | лимит размера при чтении/полле, хвост отрезается | A |
| 37 | Path traversal при применении вывода модели (api-executor): «запиши `../../…` или `.git/hooks/…`» — RCE на хосте без единого процесса гнома | запись только внутри рабочей копии, запрет `.git/**`, разрешение симлинков перед записью, контрактный тест | A |
| 38 | Checkout недоверенной ветки в ФС фабрики (симлинки, хуки, исторические CVE checkout) | инвариант: недоверенный контент материализуется только внутри окружения задачи; фабрика читает bare (git-объекты); `core.hooksPath` в пустоту во всех копиях фабрики | A |
| 39 | Отравление коробки ВНЕ рабочей копии (init-скрипты Gradle, шимы PATH, хуки клона коробки) → проверки лгут; в diff НЕ видно | ручки reuse/verify-in: fresh-коробка из коммита попытки сбрасывает всё вне ветки и доказывает самодостаточность ветки | A |
| 40 | Эксфильтрация кодированием данных в путях запросов к разрешённым реестрам | склад артефактов: гном говорит только со складом, upstream ходит сам склад | склад |
| 41 | Свежезаражённая версия пакета (s1ngularity-класс) приезжает из upstream | cooldown (версии старше N дней) + карантин/CVE-блок на складе | склад |
| 42 | Локальный клон делит объекты с клоном фабрики хардлинками → порча репо фабрики ниже уровня git | `git clone --no-hardlinks` обязателен | A |
| 43 | Тихая деградация изоляции (класс «vz молча откатился в QEMU») | самопроверка при старте проверяет и механизм изоляции, не только сеть | A |
| 44 | Репо/задача выпрашивает слабую песочницу (`executor: host` или reuse в манифесте стадии) | асимметрия владения: репо только ужесточает, привязки и ослабления — только оператор, сведение fail-closed | A |
| 45 | Серверные инструменты провайдера (web_search / web fetch на стороне API): модель делает запрос на произвольный URL инфраструктурой провайдера — мимо вахтёра и склада | политика инструментов на шлюзе: шлюз — конечная точка протокола (не MITM), видит тело запроса открытым текстом без вскрытия TLS; серверные web-инструменты вырезаются, если стадия явно не разрешила | шлюз (задел в A) |

## Сквозные принципы (проходят через весь реестр)

- **ФС-изоляция и сетевая изоляция работают только вместе** — каждая поодиночке
  обходится через другую (доктрина Anthropic из инцидентов). Отсюда: egress
  идёт в change A вместе с контейнером, не откладывается.
- **При сомнении — закрыто (fail-closed)**: песочница не поднялась /
  самопроверка не прошла / вахтёр упал → задача не стартует или
  инфраструктурный отказ, но не «исполним без защиты».
- **Гном не может менять правила собственной клетки** (read-only на конфиг
  песочницы, хуки, `.gnomish/`-закон).
- **Ни одного настоящего секрета в коробке** — только пустышка/virtual key;
  настоящие ключи и push-права живут у фабрики снаружи.
- **Prompt injection — данность** (адаптивные атаки >85% успеха): защищаемся
  средой, а не «гном не догадается».
- **Метод стоков (2026-07-31)**: данные из недоверенного источника опасны только
  в стоках, где их кто-то интерпретирует; для каждого нового канала — перечислить
  стоки и сделать каждый инертным. Обязателен в design.
- **Недоверенный контент никогда не материализуется в ФС фабрики** (2026-07-31):
  checkout — только внутри окружения задачи; фабрика читает bare и общается
  git-транспортом; фабричный git не исполняет хуки.
- **Асимметрия владения** (2026-07-31): репо декларирует потребности и может
  только ужесточать; привязка адаптеров и любые ослабления — только оператор;
  фабрика сводит fail-closed.
