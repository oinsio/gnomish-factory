package com.github.oinsio.gnomish.sandbox.environment;

/** The three Docker object types the sweep-lifecycle policy lists and classifies. */
enum ObjectKind {
    CONTAINER,
    VOLUME,
    NETWORK
}
