/**
 * The application-side consumers of the task-branch contract: what a pickup does with a classified
 * {@linkplain com.github.oinsio.gnomish.domain.branch.BranchShape shape}, and what it records about
 * having done it.
 *
 * <p>Null-marked (JSpecify): every type usage in this package is non-null by default; nullable ones
 * must carry an explicit {@code @Nullable}.
 */
@NullMarked
package com.github.oinsio.gnomish.app.branch;

import org.jspecify.annotations.NullMarked;
