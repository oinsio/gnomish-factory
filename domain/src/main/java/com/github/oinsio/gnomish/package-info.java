/**
 * Root package of the gnomish-factory walking skeleton.
 *
 * <p>Null-marked (JSpecify): every type usage in this package tree is non-null by
 * default; nullable ones must carry an explicit {@code @Nullable}. NullAway enforces
 * this at compile time (design D8, FR9 of add-project-skeleton).
 */
@NullMarked
package com.github.oinsio.gnomish;

import org.jspecify.annotations.NullMarked;
